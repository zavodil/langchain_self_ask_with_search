Based on the provided code analysis, here's the deployment configuration:


http:
  port: 8000
  health_check_path: /
name: Self-Ask with Search Agent
summary: A LangChain-based agent that performs self-ask reasoning with search capabilities, breaking down complex questions into follow-up questions and using search tools to find intermediate answers
environment_variables:
  - name: OPENAI_API_KEY
    value: <your_openai_api_key>
    description: OpenAI API key for the language model powering the agent
  - name: OPENAI_BASE_URL
    value: <optional_openai_base_url>
    description: Optional custom OpenAI API base URL (leave empty for default)
  - name: DEFAULT_MODEL_NAME
    value: gpt-3.5-turbo
    description: Default OpenAI model to use for the agent
  - name: GOOGLE_SERPER_API_KEY
    value: <your_google_serper_api_key>
    description: API key for Google Serper search service (if using Google Serper)
  - name: SEARCHAPI_API_KEY
    value: <your_searchapi_api_key>
    description: API key for SearchAPI service (if using SearchAPI)
  - name: SERPAPI_API_KEY
    value: <your_serpapi_api_key>
    description: API key for SerpAPI service (if using SerpAPI)